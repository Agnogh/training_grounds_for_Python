.venv   # ignores virtual env + installed packages
env.py  # hides files that could have secrest (can lead to alert like "“Public leak detected”.)
*.json  # hides json file as someone could take over Google account
__pycache__/    # this is for auto generated folders (something like junk files)
*.pyc   # for auto generated bycode